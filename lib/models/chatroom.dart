class ChatRoom {
  String? id;
  String name;
  // final Set<String> participants;
  // final Message message;

  ChatRoom({
    required this.id,
    required this.name,
    // required this.participants,
    // required this.message,
  });
}
